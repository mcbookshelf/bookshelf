name: 📋 Modules Review

on:
  push:
    branches: ["master"]
    paths:
      - "modules/**"
      - "src/**"
  pull_request:
    paths:
      - "modules/**"
      - "src/**"
  workflow_call:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: 🔍 Check Modules
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
      - name: Check Modules
        run: uv run check

  test:
    name: 🧪 Test Modules
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
      - name: Install Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
      - name: Cache Beet Content
        uses: actions/cache@v6
        with:
          path: .beet_cache
          key: ${{ runner.os }}-beet-cache
      - name: Test Modules
        run: uv run modules test --versions --reporter github
